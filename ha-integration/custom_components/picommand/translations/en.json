{
  "config": {
    "step": {
      "user": {
        "title": "Connect to PiCommand",
        "data": {
          "url": "PiCommand URL (e.g. http://100.127.5.18)",
          "username": "Username",
          "password": "Password"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to PiCommand",
      "invalid_auth": "Invalid username or password"
    },
    "abort": {
      "already_configured": "Already configured"
    }
  },
  "services": {
    "run_command": {
      "name": "Run Command",
      "description": "Run a shell command on a Pi node",
      "fields": {
        "node_id": {"name": "Node ID", "description": "e.g. tifftls"},
        "command": {"name": "Command", "description": "Shell command to run"},
        "timeout": {"name": "Timeout", "description": "Seconds (default 30)"}
      }
    }
  }
}
